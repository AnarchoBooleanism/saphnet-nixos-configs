################################
# 🦎 KOMODO COMPOSE - MONGO 🦎 #
################################

## This compose file will deploy:
##   1. MongoDB
##   2. Komodo Core
##   3. Komodo Periphery

services:
  mongo:
    image: mongo:8@sha256:eea8506335198f8b359865b32004036310854a935fbd317083817c614152818f
    labels:
      komodo.skip: # Prevent Komodo from stopping with StopAllContainers
    command: --quiet --wiredTigerCacheSizeGB 0.25
    # ports:
    #   - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: "${KOMODO_DB_USERNAME}"
      MONGO_INITDB_ROOT_PASSWORD: "${KOMODO_DB_PASSWORD}"
    volumes:
      - mongo-data:/data/db
      - mongo-config:/data/configdb
    networks:
      - komodo-network
    restart: unless-stopped
  
  core:
    image: ghcr.io/moghtech/komodo-core:2@sha256:518e6f4b76a9adc27b6ec0c4bb05533c8f5752986e145e3bfb64e4d15c232e32 # To only update intentionally
    container_name: komodo-core
    init: true
    depends_on:
      - mongo
    labels:
      komodo.skip: # Prevent Komodo from stopping with StopAllContainers
    ports:
      - "9120:9120"
    env_file: "${ENV_FILE}"
    environment:
      KOMODO_DATABASE_ADDRESS: mongo:27017
      KOMODO_DATABASE_USERNAME: "${KOMODO_DB_USERNAME}"
      KOMODO_DATABASE_PASSWORD: "${KOMODO_DB_PASSWORD}"
      # Key file for SOPS, to use in sops commands
      SOPS_AGE_KEY_FILE: "/root/.config/sops/age/keys.txt"
      # More custom vars
      GIT_ACCESS_TOKEN: "${GIT_ACCESS_TOKEN}"
      DOCKER_ACCESS_TOKEN: "${DOCKER_ACCESS_TOKEN}"
    volumes:
      - komodo-keys:/config/keys
      ## Store dated backups of the database - https://komo.do/docs/setup/backup
      - ${COMPOSE_KOMODO_BACKUPS_PATH}:/backups
      ## Store sync files on server
      # - /path/to/syncs:/syncs
      ## Optionally mount a custom core.config.toml
      - ${KOMODO_CONFIG_PATH}:/config/config.toml
      # SOPS key
      - ${KOMODO_SOPS_KEY_PATH}:/root/.config/sops/age/keys.txt:ro
    ## Allows for systemd Periphery connection at 
    ## "https://host.docker.internal:8120"
    # extra_hosts:
    #   - host.docker.internal:host-gateway
    networks: # For connecting with other services directly
      - komodo-network
      - central-network
    restart: unless-stopped

  ## Deploy Periphery container using this block,
  ## or deploy the Periphery binary with systemd using 
  ## https://github.com/moghtech/komodo/tree/main/scripts
  periphery:
    image: ghcr.io/nikhil-gohil/komodo-periphery-sops:v2.0.0@sha256:1674e59f7eafffe5a80b8ec1219dde364638f1f8f74f08f339667405d03c4b5a # Temporary for now until Komodo adds SOPS or we build in-house solution
    container_name: komodo-periphery
    labels:
      komodo.skip: # Prevent Komodo from stopping with StopAllContainers
    env_file: ${ENV_FILE}
    volumes:
      ## Mount external docker socket
      - /var/run/docker.sock:/var/run/docker.sock
      ## Allow Periphery to see processes outside of container
      - /proc:/proc
      ## Specify the Periphery agent root directory.
      ## Must be the same inside and outside the container,
      ## or docker will get confused. See https://github.com/moghtech/komodo/discussions/180.
      ## Default: /etc/komodo.
      - ${PERIPHERY_ROOT_DIRECTORY:-/etc/komodo}:${PERIPHERY_ROOT_DIRECTORY:-/etc/komodo}
    networks:
      - komodo-network
    restart: unless-stopped

volumes:
  # Komodo
  komodo-keys:
  # Mongo
  mongo-data:
  mongo-config:

networks:
  komodo-network: # Needed for containers to connect to each other, as central-network is listed in one of them
  central-network:
    name: central-network
    external: true
